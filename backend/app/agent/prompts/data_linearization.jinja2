TASK:
Map candidate resume facts to template FieldMappingNodes.

FIELD MAPPING NODES JSON:
{{ field_mapping_nodes_json }}

CANDIDATE RESUME FACTS:
{{ structured_data_json }}

RAW RESUME TEXT:
{{ raw_resume_text }}

OPTIONAL RECRUITER / ATS INPUT:
{{ recruiter_input_json | default("{}") }}

INSTRUCTIONS:
You are receiving a node batch from a larger template manifest.

Return one JSON object with this exact shape:

{
  "nodes": [
    {
      "node_id": "...",
      "fieldname": "...",
      "field_extraction_manifest": {
        "value_type": "scalar | rich_text | array | complex_object | array_complex",
        "value": null,
        "confidence": 0.0,
        "status": "extracted | inferred | generated_from_resume | not_found | needs_user_input",
        "reason": "...",
        "source": {
          "resume_section": "...",
          "evidence": "..."
        }
      }
    }
  ]
}

Rules:
- Return exactly one mapped node for every input node.
- Use node_id exactly as provided.
- Use fieldname exactly as provided.
- Do not use field_name.
- Do not echo marker_text, render_locator, meaning, source_hints, or field_type.
- Do not output template_fill_result.
- Do not output filled_template_manifest.
- Do not output fields.
- Do not output any wrapper except {"nodes": [...]}.
- Do not invent recruiter/ATS-only values.
- Match expected_value_shape exactly when provided in node.extract.
- For array_simple output, return a JSON array of clean strings (not newline-delimited text).
- For array_complex output, return a JSON array of objects aligned to node.sub_fields.
- Keep section semantics separate: skills = tools/technologies; professional_qualifications = certifications/licenses/degrees/courses; work_experience = role/employer/dates/responsibilities; education = degree/institution/date/grade.
- If a field is recruiter/ATS-only and missing from recruiter input, return needs_user_input.
- For paste_zone fields (copy/paste sections such as "Candidate's own CV"), do not synthesize from unrelated fields:
  - Use recruiter_input_json value when explicitly provided for that field.
  - Otherwise return null with status "needs_user_input".
  - Do not copy instruction text from template into value.
  - Keep value as plain string (no JSON object/list wrapper).

Value shape:
- scalar => scalar/null
- rich_text => string/null
- paste_zone => string/null
- array_simple => array of strings
- table_loop => array
- complex_object => object
- array_complex => array of objects

Missing values:
- scalar/rich_text/paste_zone => null
- array_simple/table_loop/array_complex => []
- complex_object => {}
- status => "not_found" or "needs_user_input"
- confidence => 0.0

Return only valid JSON. No markdown. No explanation. No code fence.
