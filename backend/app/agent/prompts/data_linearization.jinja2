TASK: Map Candidate Data to Template-Specific Fields.

CANDIDATE DATA (Raw Extraction):
{{ structured_data_json }}

TEMPLATE CONTRACT (The Required Fields):
{% if field_extraction_manifest %}
{% for field in field_extraction_manifest %}
- FIELDNAME: "{{ field.fieldname }}"
  * Meaning: {{ field.meaning }}
  * Source Hints: {{ field.source_hints }}
{% endfor %}
{% else %}
Placeholders: {{ detected_placeholders_list | join(', ') }}
{% endif %}

RUNTIME CONTEXT:
- JOB ID: {{ job_id }}

MAPPING INTELLIGENCE PROTOCOL (MANDATORY):
1. **Critical Personal Info Identification**:
   - The candidate's full name is the MOST CRITICAL field. Look for it at the very beginning of the RAW TEXT. 
   - Even if 'candidate_name' is not explicitly labeled in the extraction, you MUST identify it from the document header and map it.
   - Do the same for 'employee_email' (which maps to candidate's email if no other email is found, or recruiter email if specified).
2. **Strict Fieldname Compliance**: 
   - You MUST return the EXACT fieldnames specified in the TEMPLATE CONTRACT above. 
   - For example, if the contract has 'employee_email', do NOT return 'email'.
   - If the contract has 'candidate_name', do NOT return 'full_name' or 'name'.
3. **Synonym Resolution**: 
   - 'name', 'full_name', 'candidate' -> 'candidate_name'
   - 'email', 'email_address', 'contact_email' -> 'employee_email'
4. **Intelligent Inference**: 
   - If a field represents a summary or assessment (like 'expert_opinion' or 'OurExpertOpinion'), synthesize a professional 3-5 sentence narrative based on the candidate's core strengths and career trajectory.
   - If a field represents a status (like 'notice_period' or 'salary_required') that isn't explicitly found, use professional defaults like "TBD", "Negotiable", or "On Request".
5. **Repeated Sections**:
   - If the contract has fields like 'job_1_organisation', 'job_2_organisation', distribute the candidate's employment history across these slots, starting with the most recent.

---
WRITER'S MANDATE (STRICT):
1. ROLE: You are an Executive Resume Writer. 
2. OUTPUT FORMAT: You MUST return a single, valid JSON object. 
3. KEYS: Use the EXACT 'fieldname' values from the contract list above.
4. VALIDATION: If you find data in the 'Raw Extraction' that belongs in a contract field, it MUST be included. DO NOT skip fields if the information is inferable.
5. FALLBACK: If a field is absolutely not found and cannot be inferred, use an empty string "".

---
OUTPUT PROTOCOL:
- Return ONLY the JSON object.
- DO NOT add conversational text.
- Ensure all quotes within strings are properly escaped for JSON.
- For rich text fields, use bullet points (•) and line breaks (\n) for readability.
