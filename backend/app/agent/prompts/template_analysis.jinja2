TASK: Reverse-engineer this CV Template to suggest professional governance metadata and AI-steering guidance.

TEMPLATE TEXT (EXTRACTED):
{{ template_text }}

IDENTIFIED RAW PLACEHOLDERS:
{{ ", ".join(detected_placeholders) if detected_placeholders else "None identified programmatically." }}

Analyze the structure, visual hierarchy, expected content, and placeholders in this template. 
Generate highly specific guidance that will be used to steer other LLMs when they process actual candidate resumes using this template.

CRITICAL INSTRUCTIONS FOR PLACEHOLDERS:
- You MUST maintain a 1:1 parity between the number of markers in the doc and the number of objects in 'field_extraction_manifest'.
- There are exactly {{ detected_placeholders|length }} placeholders identified. You must output exactly {{ detected_placeholders|length }} items in the manifest.
- If you see 6 markers like '<< Fill this section >>', you MUST provide exactly 6 objects in the manifest.
- NEVER list granular sub-fields (like 'programming_languages') if the template only has one high-level box for that section. Infer semantics from headers and structure.
- The manifest objects MUST appear in the EXACT ORDER they appear in the visual text of the template.
- Do not provide generic resume advice. Only provide instructions specific to populating this exact template based on its layout and headers.

Provide recommendations in the following JSON format:
1. 'purpose': A concise description of the ideal profile for this template.
2. 'expected_sections': A comma-separated string of the literal headers found.
3. 'field_extraction_manifest': A list of objects, one for each physical marker in the doc.
   Each object MUST have:
   - 'fieldname': The unique logical key for the marker (e.g., 'technical_skills').
   - 'meaning': A brief description of what this section represents visually in the template.
   - 'field_type': The type of field. MUST be one of: narrative | list | timeline | grouped_list | structured | hybrid
   - 'field_intent': What kind of information should be conveyed here (e.g., 'To show career progression', 'To list hard skills').
   - 'source_hints': A comma-separated string of specific resume data points that should be "sourced" to fill this field (e.g., 'programming languages, cloud platforms, databases').
   - 'content_expectation': What kind of content is expected (e.g., 'Action-oriented bullet points', 'A short paragraph').
   - 'structure_expectation': Expected structure (e.g., 'Bullet points starting with verbs', 'Key-value pairs').
   - 'constraints': Any constraints (e.g., 'Maximum 5 bullet points', 'Do not use first person').
   - 'confidence': (Optional) Float between 0.0 and 1.0 indicating confidence in the inferred semantics.
   - 'ambiguity_note': (Optional) A brief string explaining any uncertainty about this field's purpose.
4. 'global_guidance': Specific rules for how the AI should approach filling this template overall.

OUTPUT FORMAT: Return ONLY a valid JSON object. No markdown, no preamble.
