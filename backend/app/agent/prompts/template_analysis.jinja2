TASK: Analyze a Resume Template and Generate a Field Extraction Manifest.

TEMPLATE CONTEXT (Text Extraction):
{{ template_text }}

DETECTED MARKERS (The ONLY valid strings for marker_text):
{{ detected_placeholders | join(', ') }}

INSTRUCTIONS:
1. **EXCLUSIVE SOURCE OF TRUTH**: Every 'marker_text' you provide MUST exist exactly as-is in the "DETECTED MARKERS" list above.
2. **STRICT PROHIBITION**: DO NOT use bold text (e.g., "**Salary required**"), headers, or labels from the TEMPLATE CONTEXT as a 'marker_text'. These are LABELS, not placeholders.
3. **MANDATORY MAPPING**: For each logical field (e.g., 'candidate_name'), identify its LABEL in the text and pick the NEAREST valid string from the "DETECTED MARKERS" list to be its 'marker_text'.
4. **TABLE LABEL→VALUE PAIRS**: If the template context contains a "[STRUCTURED TABLE LABEL→VALUE PAIRS]" section, use it directly. The VALUE/MARKER column shows what sits next to each LABEL — that adjacent VALUE/MARKER is the placeholder you must assign as 'marker_text'. This is your highest-priority mapping source.
5. **NO HALLUCINATION**: If a field does not have a corresponding marker in the "DETECTED MARKERS" list, set marker_text to "" — do NOT invent one.
6. **HEADER FIELDS**: For fields like 'candidate_name' and 'candidate_id', look for literal markers at the top of the document (e.g., "«CandidateFullName»").
7. **GENERIC MARKERS**: If you see generic markers like "[Type text]" appearing multiple times in the "DETECTED MARKERS" list, map them in the order they appear in the text to the most logical fields based on the surrounding labels.
8. **COMPLETE COVERAGE**: Every marker in the "DETECTED MARKERS" list must be assigned to exactly one fieldname. Omitting a known marker is a failure.

Provide recommendations in the following JSON format:
{
  "purpose": "Concise description",
  "expected_sections": "Header, Experience, etc",
  "expected_fields": "List of all fieldnames",
  "summary_guidance": "Instructions for AI summary",
  "formatting_guidance": "Rules for data formatting",
  "validation_guidance": "Critical checks to perform on the final output",
  "field_extraction_manifest": [
    {
      "fieldname": "snake_case_name",
      "marker_text": "EXACT_STRING_FROM_DETECTED_MARKERS_LIST",
      "meaning": "Description",
      "source_hints": "Where to find in resume"
    }
  ]
}
