TASK: Reverse-engineer this CV Template to suggest professional governance metadata and AI-steering guidance.

TEMPLATE TEXT (EXTRACTED):
{{ template_text }}

DOCLING STRUCTURAL ANALYSIS (LAYOUT MAP):
{{ docling_metadata_json }}

IDENTIFIED RAW PLACEHOLDERS:
{{ ", ".join(detected_placeholders) if detected_placeholders else "None identified programmatically." }}

---
ANALYSIS CONTEXT:
We are using a Dual-Stream extraction process:
1. "Visual Stream": The markers you see within the TEMPLATE TEXT.
2. "Deep XML Stream": Hidden markers (MergeFields/Metadata) identified programmatically from the file's internal XML structure. These may appear as a [SYSTEM: HIDDEN METADATA MARKERS] block at the top of the text.

YOUR GOAL:
Generate a unified manifest that includes EVERY SINGLE placeholder from BOTH streams.

CRITICAL INSTRUCTION:
- You MUST maintain a 1:1 parity between EVERY marker identified in the "IDENTIFIED RAW PLACEHOLDERS" list and the entries in 'field_extraction_manifest'.
- Hidden metadata markers (e.g., «CandidateID») MUST be included in the manifest. Even if you don't see them in the visual paragraphs, assume they are anchor points at the very top of the document (usually in the header or the first row of the first table).
- Map each placeholder to a unique, descriptive logical key (snake_case) as the 'fieldname'.
- Omission of ANY detected placeholder is a CRITICAL FAILURE.

Provide recommendations in the following JSON format:
1. 'purpose': A concise description of the ideal profile for this template.
2. 'expected_sections': A comma-separated string of the literal headers found.
3. 'field_extraction_manifest': A list of objects, one for each physical marker in the doc.
   Each object MUST have:
   - 'fieldname': The unique logical key for the marker (e.g., 'candidate_id').
   - 'marker_text': The literal text found in the template (e.g., '«CandidateID»' or '[Type text]').
   - 'visual_context': Physical location (e.g., "Top header table, row 1" or "Under 'Professional Qualifications'").
   - 'meaning': Description of the field's intent.
   - 'source_hints': Comma-separated resume data points to fill this field.
4. 'summary_guidance': Style rules for the summary.
5. 'formatting_guidance': Specific rules for how the AI should format the harmonized text for these fields.

OUTPUT FORMAT: Return ONLY a valid JSON object. No markdown, no preamble.
