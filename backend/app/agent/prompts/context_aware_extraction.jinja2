TASK: Transform the resume into the target structured format. 
Ensure 100% semantic alignment with the target schema.

TARGET SCHEMA (Keys to fill):
{{ dynamic_schema_json }}

FIELD EXTRACTION MANIFEST (Source-to-Target mapping):
{{ field_extraction_manifest_json }}

TARGET TEMPLATE CONTEXT (Visual structure):
{{ template_text_excerpt }}

REFINED EXTRACTION CONTEXT (From parsing tools):
{{ structured_context }}

RAW RESUME TEXT (Source data):
{{ extracted_text }}

SPECIFIC FORMATTING GUIDANCE:
{{ formatting_guidance }}

STRICT EXTRACTION RULES:
1. SEPARATION OF CONTEXT: 
   - 'professional_experience': ONLY roles at companies. (NO DEGREES).
   - 'education': ONLY Universities and Degrees. (Do NOT put these in experience).
   - 'certifications': ONLY professional certs (AWS, Google, etc.).
   - 'skills': Technical skills (C#, Java, GCP).

2. SCHEMATIC ALIGNMENT:
   - Use ONLY the keys provided in the TARGET SCHEMA.
   - If the schema has a key for "certifications", do not put them in "education".
   - Use the FIELD EXTRACTION MANIFEST to map obscure raw sections to specific target fields.

3. DATA HYDRATION:
   - Prioritize structured context for dates and company names.
   - Maintain the original bullet points if they are technical.
   - Do NOT omit any relevant facts from the source text.

4. JSON INTEGRITY:
   - Output ONLY a single, valid JSON object. 
   - No preamble, no markdown formatting around the JSON, just the raw code.
