TASK: Perform a Semantic & Structural Validation of the formatted resume.

SOURCE TEXT (RAW RESUME):
{{ extracted_text }}

TRANSFORMED DATA (JSON MANIFEST):
{{ transformed_data_json }}

LINEARIZED NARRATIVE (FINAL FORMATTED TEXT):
{{ linearized_data }}

TEMPLATE GUIDANCE:
{{ guidance }}

INSTRUCTIONS:
1. **DATA FIDELITY CHECK (CRITICAL)**: 
   - **EXTRACTION FAILURE**: If the SOURCE TEXT mentions a Degree, School, or Certification that is NOT in the TRANSFORMED DATA, you MUST [[STATUS :: FAIL]].
   - **LEGITIMATE ABSENCE**: If a template field (e.g., Certifications) is empty because the SOURCE TEXT truly contains no such information, this is a [[STATUS :: PASS]]. 
   - In the report, clearly note when data was absent in the source (e.g., "Candidate has no certifications in source resume").

2. **LINEARIZATION & FORMATTING CHECK**:
   - **NO WALLS OF TEXT**: Does 'Professional Experience' look like a single block in the LINEARIZED NARRATIVE? If it lacks [:L1:] markers or newlines between jobs, it is a failure.
   - **NO HALLUCINATED PLACEHOLDERS**: Check for literal placeholders like '[Institution Name]', '[Dates]', or '[Degree]'. If these bracketed words are present (indicating the AI copied examples from your instructions instead of using source data), you MUST [[STATUS :: FAIL]].
   - **EDUCATION COMPLETENESS**: Ensure university names and dates appear in the narrative if they were in the source.
   - **TAG INTEGRITY**: Check for raw JSON residue or missing visual protocol tags like [:B:], [:PIPE:], etc.


3. **OUTPUT PROTOCOL**: 
   - Overall status: [[STATUS :: PASS]] or [[STATUS :: FAIL]]
   - Reasoning/Feedback: [[REPORT :: ...detailed reasoning...]]

GOAL: Zero data loss. Premium formatting. Transparent quality.

